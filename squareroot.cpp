#include<iostream>
using namespace std;

int main()
{
	int number,root;
	cout<<"enter number whose root is to be found:";
	cin>>number;
	root=sqrt(number);
	cout<,"square root of "<<number<<"is"<<root;
	return 0;
}